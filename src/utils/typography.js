import Typography from "typography";
import grandViewTheme from "typography-theme-lawton";

const typography = new Typography(grandViewTheme);

export const { rhythm, scale } = typography;
export default typography;
